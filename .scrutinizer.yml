build:
    nodes:
        php72:
            environment:
                php:
                    version: 7.2
            tests:
                override:
                    -
                        command: 'vendor/bin/phpunit --coverage-clover=coverage.clover'
                        coverage:
                            file: 'coverage.clover'
                            format: 'clover'

        php73:
            environment:
                php:
                    version: 7.3
            tests:
                override:
                    -
                        command: 'vendor/bin/phpunit --coverage-clover=coverage.clover'
                        coverage:
                            file: 'coverage.clover'
                            format: 'clover'

        php74:
            environment:
                php:
                    version: 7.4
            tests:
                override:
                    -
                        command: 'vendor/bin/phpunit --coverage-clover=coverage.clover'
                        coverage:
                            file: 'coverage.clover'
                            format: 'clover'
